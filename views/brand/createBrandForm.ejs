<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Cadastrar Empresa</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
  <style>
    * {
      box-sizing: border-box;
      font-family: Arial, Helvetica, sans-serif;
    }

    body {
      margin: 0;
      min-height: 100vh;
      background: #fde7e7;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .container {
      width: 100%;
      max-width: 420px;
      background: #f5f5f5;
      padding: 24px;
      border-radius: 10px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.15);
      position: relative;
    }

    .back-btn {
      position: absolute;
      top: 12px;
      left: 12px;
      background: transparent;
      border: none;
      font-size: 20px;
      cursor: pointer;
      color: #333;
    }

    .back-btn:hover {
      color: #000;
    }

    .title {
      background: #6fa8dc;
      color: #fff;
      text-align: center;
      padding: 10px;
      border-radius: 6px;
      margin-bottom: 20px;
      font-weight: bold;
    }

    .form-group {
      display: flex;
      align-items: center;
      margin-bottom: 14px;
      gap: 10px;
    }

    .form-group label {
      min-width: 80px;
      font-weight: bold;
      color: #333;
    }

    .form-group input {
      flex: 1;
      padding: 8px 10px;
      border-radius: 6px;
      border: 1px solid #ccc;
      background: #fff;
    }

    .form-group input:disabled {
      background: #e0e0e0;
      color: #777;
      cursor: not-allowed;
    }

    .actions {
      display: flex;
      justify-content: center;
      margin-top: 20px;
    }

    .btn {
      background: #7ed957;
      border: none;
      color: #1b4d1b;
      padding: 10px 18px;
      border-radius: 8px;
      font-weight: bold;
      cursor: pointer;
      transition: 0.2s ease;
    }

    .btn:hover {
      filter: brightness(0.95);
    }

    a{
        text-decoration: none;
        text-transform: none;
        color: black;
    }

    @media (max-width: 480px) {
      .container {
        margin: 0 12px;
        padding: 18px;
      }

      .form-group label {
        min-width: 70px;
        font-size: 14px;
      }
    }
  </style>
</head>
<body>
  <div class="container">
      <a href="/dashboard" class="back-btn" type="button">
        <i class="bi bi-arrow-left"></i>
      </a>
    <div class="title">Cadastrar uma Empresa</div>

    <form action="/brand/add" method="post">
      <div class="form-group">
        <label for="name"><i class="bi bi-building"></i> Nome:</label>
        <input type="text" name="name" id="name" placeholder="Nome da empresa" />
      </div>

      <div class="form-group">
        <label for="vendedor"><i class="bi bi-person"></i> Vendedor:</label>
        <input type="text" id="vendedor" value="Indisponível" disabled />
      </div>

      <div class="actions">
        <button type="submit" class="btn"><i class="bi bi-plus-circle"></i> Adicionar Empresa</button>
      </div>
    </form>
  </div>
</body>
</html>
